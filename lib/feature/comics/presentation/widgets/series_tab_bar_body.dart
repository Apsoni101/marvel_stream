import 'package:flutter/material.dart';

class SeriesTabBarBody extends StatelessWidget {
  const SeriesTabBarBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('New series Content'));
  }
}
